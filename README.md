# Specular starter

A counter example written in specular.

## Running

- `npm install`

Add `node_modules/.bin` to path:
- `export PATH="./node_modules/.bin:$PATH"`

- `spago bundle-app`

- `open index.html`
